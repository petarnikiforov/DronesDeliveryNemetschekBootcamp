export class Order {
    constructor(customerId, productList, warehouse) {
        this.customerId = customerId;
        this.productList = productList;
        this.warehouseId = warehouseId;
    }
}